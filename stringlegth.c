// C program to find the length of string
#include <stdio.h>
#include <string.h>
 
int main(){
    char Str1[100]="descendantsofthesun";
    int i = strlen(Str1);
 
    printf("Length of Str is %u", i);

}